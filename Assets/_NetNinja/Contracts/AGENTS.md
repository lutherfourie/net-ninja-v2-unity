# NetNinja.Contracts

Engine-free boundary types only. No UnityEngine. Double-only. Analyzer-guarded.

- Interfaces + event structs + `Vec3` + `InputFrame` + `StateSnapshot` live HERE.
- Implementations live in Core. Do not duplicate.
